@echo off
netsh wlan show profiles
for /f "tokens=1,2 delims=:" %%i in ('netsh wlan show profiles') do (
  if "%%i"=="    All User Profile" (
    echo.
    echo Profile :%%j
    netsh wlan show profile name="%%j" key=clear | findstr Key
    echo.
  )
)
pause